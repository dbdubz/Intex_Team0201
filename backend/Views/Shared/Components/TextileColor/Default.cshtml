@{
}

@model IEnumerable<string>
<div class="d-grid gap-2">
    <h6>Textile Color</h6>
    <select>
        @foreach (string t in Model)
        {
            <option asp-route-TextileColor="@t">@t</option>
        }
    </select>

</div>