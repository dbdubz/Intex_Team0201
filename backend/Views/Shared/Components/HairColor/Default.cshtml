@{
}

@model IEnumerable<string>
<div class="d-grid gap-2">
    <h6>Hair Color</h6>
    <select>
        @foreach (string t in Model)
        {
            <option asp-route-HairColor="@t">@t</option>
        }
    </select>

</div>