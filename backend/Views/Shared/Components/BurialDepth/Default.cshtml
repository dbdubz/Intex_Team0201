@{
}

@model IEnumerable<string>
<div class="d-grid gap-2">
    <h6>Burial Depth</h6>
    <select>
        @foreach (string t in Model)
        {
            <option asp-route-BurialDepth="@t">@t</option>
        }
    </select>

</div>