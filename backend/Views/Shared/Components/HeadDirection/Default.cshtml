@{
}

@model IEnumerable<string>
<div class="d-grid gap-2">
    <h6>Head Direction</h6>
    <select>
        @foreach (string t in Model)
        {
            <option asp-route-HeadDirection="@t">@t</option>
        }
    </select>

</div>